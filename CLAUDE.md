@AGENTS.md

# כללי תוכן — מאמרים

- **סיומת URL (slug)**: לכל מאמר חדש חייבת להיות סיומת URL בעברית, שנגזרת מהכותרת/הנושא — לא אנגלית ולא תעתיק. לדוגמה: הכותרת "דוחות שמתעדכנים לבד" → הסיומת צריכה להיות עברית (כמו `דוחות-שמתעדכנים-לבד`), לא `automated-reports-guide`. **הערה טכנית**: כדי שזה יעבוד, `src/app/articles/[slug]/page.tsx` (וכל מקום דומה שמקבל `slug` מ-`params`) חייב לעטוף אותו ב-`decodeURIComponent(slug)` לפני שמעבירים אותו ל-`getArticleBySlug` — בלי זה, סיומות עבריות (לא-ASCII) גורמות ל-404 עקבי (נבדק ותוקן ב-26.8.2026).
- **תאריך פרסום**: כשמעלים מאמר חדש, שדה ה-`date` בפרונט-מאטר חייב להיות התאריך הנוכחי (היום) — לא מוקדם ולא מאוחר יותר — אלא אם המשתמש מבקש תאריך אחר במפורש.
- **תמונות**: אין תמונות של נשים — בשום תמונה באתר (תמונות נושא של מאמרים, מדיה בתיק העבודות, גרפיקה בעמודים). גם אין תמונות פנים/אנשים בכלל, בהתאם לעקרון האנונימיות של האתר. תמונת הנושא שנוצרת אוטומטית למאמר היא וקטורית-גאומטרית בלבד.

# הנחיות עבודה על הפרויקט

## עבודה משני מחשבים - חובה לסנכרן דרך Git

הפרויקט הזה נערך משני מחשבים שונים. כדי שהעבודה תישאר מסונכרנת, יש לפעול לפי הכללים הבאים בכל סשן עבודה:

### בתחילת כל סשן עבודה
לפני שמתחילים לערוך קבצים, למשוך את הגרסה העדכנית ביותר מהריפו המרוחק:
```
git pull
```
אם יש קונפליקטים (conflicts) - לעצור ולהציג אותם למשתמש, לא לפתור אוטומטית בלי אישור.

### בסוף כל שינוי משמעותי
אחרי כל שינוי או קבוצת שינויים הגיוניים, יש לשמור ולדחוף מיד - לא להסתפק ב-commit מקומי בלבד:
```
git add -A
git commit -m "<תיאור קצר וברור של השינוי>"
git push
```

**חשוב:** `commit` בלי `push` לא מספיק. השינויים חייבים להגיע לריפו המרוחק, אחרת הם לא יהיו זמינים במחשב השני.

### אם push נכשל
אם ה-push נדחה כי יש שינויים חדשים בריפו המרוחק שלא קיימים מקומית (למשל כי נעשתה עבודה מהמחשב השני), יש לבצע `git pull` (עם rebase אם אפשר), לוודא שאין קונפליקטים, ורק אז לנסות push שוב. אם יש קונפליקט אמיתי - לעצור ולהסביר למשתמש מה הבעיה, לא לבחור צד באופן שרירותי.

### לפני סיום סשן עבודה
לוודא שאין שינויים לא-שמורים (`git status` נקי) ושבוצע push אחרון מוצלח.
